[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: hits the real OpenAI API via crewai (costs money/time; opt-in only)
filterwarnings =
    ignore::DeprecationWarning:crewai.*
    ignore:function callbacks cannot be serialized.*:UserWarning
